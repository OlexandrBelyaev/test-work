{
  "homepage": "https://github.com/OlexandrBelyaev/test-work.git",
  "name": "testdeploy",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "start": "parcel ./index.html --open",
    "build": "npx build /index.html --dist-dir ./docs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  },
  "dependencies": {
    "eslint": "^7.23.0"
  }
}
